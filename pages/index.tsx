import Layout from '../components/Dashboard/Layout';

export const Home = (): JSX.Element => {
    return (
        <Layout>
            <h1>Hello World</h1>
        </Layout>
    );
};

export default Home;
